
name: Manevi Kale IPA Pişirici
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Kodları Çek
        uses: actions/checkout@v4 # v3 yerine v4 yaptık

      - name: Node.js Kur
        uses: actions/setup-node@v4 # v3 yerine v4 yaptık
        with:
          node-version: 18
          cache: 'npm'

      - name: Modülleri Yükle
        run: npm install

      - name: Expo Kur
        run: npm install -g eas-cli

      - name: iOS Projesini Hazırla
        run: npx expo export --platform ios

      - name: IPA Dosyasını Paketle ve Sakla
        uses: actions/upload-artifact@v4 # v3 yerine v4 yaptık
        with:
          name: Manevi-Kale-IPA
          path: dist/
